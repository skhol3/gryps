# gryps — Video Analytics Microkernel

Edge and cloud video analytics framework.

## Dev

```shell
uv sync --group dev
uv run pre-commit install
uv run pytest -v
uv run ruff check .
uv run mypy .
```
