# OpenSpec SDD Configuration — gryps
# Auto-generated by sdd-init (engram mode)
# Last updated: 2026-06-10

project: gryps
version: "0.1.0"
description: >
  Edge-first video-analytics platform for LPR (License Plate Recognition).
  Python 3.12, uv-managed. Modular microkernel architecture with Event Bus,
  plugin-based detectors/OCR/preprocessors, and hardware profiles
  (EDGE_HOUSE, EDGE_NVR, EDGE_RPI, CLOUD_HUB).
  Design docs in docs/ (ARQUITECTURA.md, VISION.md, HISTORIAS_USUARIO.md).
target_runtime: python
package_manager: uv
python_version: ">=3.12"

persistence:
  mode: engram
  engram_project: gryps
  openspec_root: openspec/

strict_tdd: true
strict_tdd_reason: >
  Test runner detected, no explicit marker disables strict TDD.
  SDD apply and verify phases must follow strict TDD.

testing:
  runner:
    available: true
    command: uv run pytest -v --cov=gryps tests/
    framework: pytest 9.0.3
  layers:
    unit: true
    integration: false
    e2e: false
  coverage:
    available: true
    command: uv run pytest --cov=gryps
    tool: pytest-cov 7.1.0
    current: 92%
  quality:
    linter:
      available: true
      command: uv run ruff check .
    type_checker:
      available: true
      command: uv run mypy .
      mode: strict
    formatter:
      available: true
      command: uv run ruff format .

adr:
  source: docs/ARQUITECTURA.md
  count: 10

user_stories:
  source: docs/HISTORIAS_USUARIO.md
  count: 28

phases:
  proposal:
    required: true
    template: openspec/proposals/{change_id}.md
  spec:
    required: true
    template: openspec/specs/{change_id}/README.md
  design:
    required: true
    template: openspec/designs/{change_id}/README.md
  tasks:
    required: true
    output: openspec/changes/{change_id}/tasks.yaml
  apply:
    requires_tests: true
  verify:
    requires_tests: true
  archive:
    archive_dir: openspec/changes/archive/{change_id}/

chained_pr_strategy: ask-always
review_budget_lines: 400
execution_mode: interactive
